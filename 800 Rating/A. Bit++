// Problem: A. Bit++
// Rating: 800

#include <bits/stdc++.h>
using namespace std;
 
int main() 
{
  int n,a,x=0;
  string s;
  cin>>n;
  while(n--)
  {
    cin>>s;
    if(s.find("++")!=string::npos)
    x++;
    else
    x--;
  }
    cout<<x<<endl;
    
    return 0;
}
